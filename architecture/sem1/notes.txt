1. В python id - имя встроенной функции, поэтому я заменил имя переменных в классах на id_

2. Не указано откуда берутся типы Point3D, Angle3D и Color и как они взаимодействуют с
   другими классами, поэтому сделал заглушки самостоятельно определил взаимодействие.

3. Camera агрегируется в Scene, но не фигурирует там в качестве поля или аргумента метода.
   Не понятно, для чего эта агрегация и как ее реализовать.

4. Для реализации классов Flash и Camera можно было бы создать интерфейс или абстрактный класс,
   в котором указать методы Rotate() и Move(), от которого потом эти классы наследовать.

5. Не понятно Type в классе Scene - это один тип данных во всех упоминаниях или абстрактная заглушка,
   которая потом заменится на разные типы. Остановился на первом.

6. Неправильно указано взаимодействие классов PoligonalModel c Texture и Poligon.
   Насколько я понимаю, направление стрелок должно быть противоположным.